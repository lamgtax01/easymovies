export const Button = ({children}) => {
  return (
    <button className="w-72 text-2xl text-white font-semibold bg-gradient-to-r from-cyan-500 to-blue-500 hover:text-yellow-200 p-4 rounded-md bg-slate-300 ">{children}</button>
  )
}
